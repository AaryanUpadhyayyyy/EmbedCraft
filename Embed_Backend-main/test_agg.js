const mongoose = require('mongoose'); mongoose.connect('mongodb://localhost:27017/embedcraft').then(async () => { const EventLog = mongoose.model('EventLog', new mongoose.Schema({}, { strict: false })); try { const aggregatedStats = await EventLog.aggregate([ { $facet: { audience: [ { $match: { metadata: { $exists: true, $type: 'object' } } }, { $project: { kv: { $objectToArray: '$metadata' } } }, { $unwind: '$kv' }, { $match: { $or: [ { 'kv.v': { $type: 'string' } }, { 'kv.v': { $type: 'number' } }, { 'kv.v': { $type: 'boolean' } } ] } }, { $group: { _id: { key: '$kv.k', value: '$kv.v' }, count: { $sum: 1 } } } ] } } ]); console.log('Agg:', JSON.stringify(aggregatedStats)); } catch(e) { console.log('ERROR:', e.message); } process.exit(0); });
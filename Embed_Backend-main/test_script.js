const mongoose = require('mongoose'); mongoose.connect('mongodb://localhost:27017/embedcraft').then(async () => { const EventLog = mongoose.model('EventLog', new mongoose.Schema({ nudge_id: String, event_type: String, metadata: Object }, { strict: false })); const docs = await EventLog.find({ nudge_id: { $exists: true } }).sort({_id: -1}).limit(5).lean(); console.log(JSON.stringify(docs, null, 2)); process.exit(0); });